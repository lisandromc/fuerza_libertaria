<%= form_for(@usuario) do |f| %>
  <div class="columns">
    <div class="column">
      <div class="field">
        <label class="label">Nombre*</label>
        <div class="control has-icons-left">
          <%= f.text_field(:nombre, class: 'input', required: true, placeholder: 'Nombre') %>
          <span class="icon is-small is-left">
            <i class="fa fa-user"></i>
          </span>
        </div>
        <% if @usuario.errors[:nombre].any? %>
          <li class="help is-danger"><%= @usuario.errors[:nombre].join(', ') %></li>
        <% end %>
      </div>
      <div class="field">
        <label class="label">Email*</label>
        <div class="control has-icons-left">
          <%= f.email_field(:email, class: 'input', required: true, placeholder: 'Email') %>
          <span class="icon is-small is-left">
            <i class="fa fa-envelope"></i>
          </span>
        </div>
        <% if @usuario.errors[:email].any? %>
          <li class="help is-danger"><%= @usuario.errors[:email].join(', ') %></li>
        <% end %>
      </div>
      <% if @usuario.new_record? %>
        <div class="field">
          <label class="label">Password*</label>
          <div class="control has-icons-left">
            <%= f.password_field(:password, class: 'input', required: true, placeholder: 'Password') %>
            <span class="icon is-small is-left">
              <i class="fa fa-unlock-alt"></i>
            </span>
          </div>
          <% if @usuario.errors[:password].any? %>
            <li class="help is-danger"><%= @usuario.errors[:password].join(', ') %></li>
          <% end %>
        </div>
      <% end %>
      <div class="field">
        <label class="label">Profesión</label>
        <div class="control has-icons-left">
          <%= f.text_field(:profesion, class: 'input', placeholder: 'Profesión') %>
          <span class="icon is-small is-left">
            <i class="fa fa-briefcase"></i>
          </span>
        </div>
      </div>
      <div class="field">
        <label class="label">Configuración</label>
        <div class="control">
          <label class="checkbox">
            <%= f.check_box(:activo_mapa_liberal) %>
            Quiero aparecer en el Mapa Liberal
          </label>
        </div>
      </div>
    </div>
    <div class="column">
      <div class="field">
        <label class="label">Áreas de conocimiento</label>
        <div class="control has-icons-left">
          <%= f.text_field(:areas_conocimiento, class: 'input', placeholder: 'Tecnología, marketing, marketing') %>
          <span class="icon is-small is-left">
            <i class="fa fa-eye"></i>
          </span>
        </div>
      </div>
      <div class="field">
        <label class="label">Móvil</label>
        <div class="control has-icons-left">
          <%= f.text_field(:movil, class: 'input', placeholder: 'Móvil') %>
          <span class="icon is-small is-left">
            <i class="fa fa-mobile"></i>
          </span>
        </div>
      </div>
      <div class="field">
        <label class="label">DNI</label>
        <div class="control has-icons-left">
          <%= f.text_field(:dni, class: 'input', pattern: '[0-9]{7,8}',placeholder: 'DNI') %>
          <span class="icon is-small is-left">
            <i class="fa fa-address-card-o"></i>
          </span>
        </div>
        <% if @usuario.errors[:dni].any? %>
          <li class="help is-danger"><%= @usuario.errors[:dni].join(', ') %></li>
        <% end %>
      </div>
      <div class="field">
        <label class="label">Domicilio</label>
        <div class="control has-icons-left">
          <%= f.text_field(:domicilio, class: 'input', placeholder: 'Domicilio') %>
          <span class="icon is-small is-left">
            <i class="fa fa-home"></i>
          </span>
        </div>
        <% if @usuario.errors[:domicilio].any? %>
          <li class="help is-danger"><%= @usuario.errors[:domicilio].join(', ') %></li>
        <% end %>
      </div>
      <div class="field">
        <label class="label">Localidad*</label>
        <div class="control has-icons-left">
          <%= f.text_field(:localidad, class: 'input', required: true, placeholder: 'Localidad') %>
          <span class="icon is-small is-left">
            <i class="fa fa-map-marker"></i>
          </span>
        </div>
        <% if @usuario.errors[:localidad].any? %>
          <li class="help is-danger"><%= @usuario.errors[:localidad].join(', ') %></li>
        <% end %>
      </div>
      <p class="help">
        - El DNI y Domicilio son opcionales para unirte al grupo, pero necesarios para la presentación de adherentes en la
        junta electora, paso requerido para la personería jurídica del partido político.<br />
        - Profesión y áreas de conocimiento son opcionales, pero nos ayudarán a saber con que expertise cuentas para
        colaborar activamente.
      </p>
    </div>
  </div>
  <hr />
  <div class="has-text-right">
    <button type="submit" class="button is-link is-medium"><%= submit_title %></button>
  </div>
<% end %>
